context("reactive Struct", () => {
  specify("every destructured value is reactive");

  describe("changing properties value via `=`", () => {
    it("same as changing value via `.set()`");
    it("same as changing value via `.let = `");
  });

  it("can specify the depth");
});
